extends ../layout

block content
  h1= title

  ul
    each player in player_list
      li
        a(href=player.url) #{player.nickname}
        |  (#{player.team.name}, 
        |  #{player.role.name})

    else
      li There are no player.
