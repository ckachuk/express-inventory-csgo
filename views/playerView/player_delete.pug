extends ../layout

block content
  h1 #{title}: #{player.nickname}
  p= player.full_name

  p Do you really want to delete this book?

  form(method='POST' action='')
    div.form-group
      input#bookid.form-control(type='hidden',name='playerid', required='true', value=player._id )
      button.btn.btn-primary(type='submit') Delete